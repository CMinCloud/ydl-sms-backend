ydl:
  redis:
    ip: 47.109.59.20
    port: 6380
    password: 403630
    database: 0
spring:
  cache:
    type: GENERIC
  redis:
    host: ${ydl.redis.ip}
    password: ${ydl.redis.password}
    port: ${ydl.redis.port}
    database: ${ydl.redis.database}